name: Keepalive Workflow
on:
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:

jobs:
  保活:
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: 重新启用工作流
        uses: liskin/gh-workflow-keepalive@v1
